'use strict';

// Thin wrapper — page routes are in dashboardRoutes.js
// This file left intentionally slim for future server-management UI pages
const express = require('express');
const router  = express.Router();
module.exports = router;
